import { Card, CardContent } from "@/components/ui/card"

export default function AboutSection() {
  return (
    <Card className="p-6">
      <CardContent className="p-0 text-sm sm:text-base">
        {/* About content intentionally left blank as requested. */}
      </CardContent>
    </Card>
  )
}

